<div class="d-flex justify-content-between align-items-center">
<h1>Editing grinder</h1>
<%= button_to "Destroy this grinder", @grinder, method: :delete, class: 'btn btn-danger' %>
  </div>

<%= render "form", grinder: @grinder %>

<br>

<div class="m-3 text-end">
  <%= link_to "Back to grinders", grinders_path, class: 'btn btn-secondary' %>
</div>
